package newthing;

import javax.swing.*;

public class MyGameGraphics extends JFrame{
    public MyGameGraphics(){
        JFrame window = new JFrame();
        window.setSize(1080,680);
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        window.setVisible(true);
    }
}